{
  "response": [
    {
      "name": "Region Feature",
      "slug": "region-feature",
      "isEnabled": true,
      "segments": [
        { "key": "region", "value": "us" },
        { "key": "region", "value": "au" }
      ]
    }
  ],
  "cases": [
    {
      "description": "enabled when context matches the first of two values for the same segment key",
      "configuration": {
        "slug": "region-feature",
        "context": { "region": "us" },
        "defaultValue": false
      },
      "expected": {
        "value": true
      }
    },
    {
      "description": "enabled when context matches the second of two values for the same segment key",
      "configuration": {
        "slug": "region-feature",
        "context": { "region": "au" },
        "defaultValue": false
      },
      "expected": {
        "value": true
      }
    },
    {
      "description": "disabled when context has the same segment key but a non-matching value",
      "configuration": {
        "slug": "region-feature",
        "context": { "region": "eu" },
        "defaultValue": false
      },
      "expected": {
        "value": false
      }
    },
    {
      "description": "disabled when context does not contain the required segment key",
      "configuration": {
        "slug": "region-feature",
        "context": { "license": "trial" },
        "defaultValue": false
      },
      "expected": {
        "value": false
      }
    }
  ]
}
